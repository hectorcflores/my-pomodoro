# hectorcflores.github.io

Personal hub page linking to my apps (Pomodoro, Reading List), with each app's setup
link remembered per browser so I don't have to keep the long key URLs around.

Setup links are stored only in each browser's `localStorage` — keep the raw URLs in a
password manager as the source of truth. To add an app, edit the `APPS` array in
`index.html`.
